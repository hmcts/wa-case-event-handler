java:
  # Don't modify below here
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}

  servicebus:
    resourceGroup: wa
    enabled: true
    teamName: WA
    serviceplan: standard
    setup:
      topics:
        - name: ccd-case-events-test
          subscriptionNeeded: yes
